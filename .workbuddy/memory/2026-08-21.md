# 2026-08-21

## 工作台新增数据导入导出功能并重新打包 exe

- 新增 `backend/routers/data_io.py`：
  - `GET /api/data/export?format=json|csv`：导出全部工作，CSV 带 UTF-8 BOM + 中文表头，Content-Disposition 带中文文件名；
  - `POST /api/data/import`（multipart）：支持 JSON/CSV，mode=merge（名称+类型+计划日期相同判重跳过）/ replace（清空后写入）；CSV 中英文表头均可识别（HEADER_ALIASES），支持 UTF-8/GBK 解码，无效行跳过并计数；已用 curl 全场景验证（merge 去重、replace、错误文件、坏表头、坏 JSON）。
- 前端：`api.js` 加 exportData（blob 下载+解析 filename*）/importData（FormData）；顶栏加「⇧导入 / ⇩导出」按钮；`modal.js` 加 openExport/openImport（文件选择+拖拽、模式切换、replace 二次确认）；`style.css` 加 .io-* 样式。
- 文档：README 功能表/接口概览、docs/API文档.md 第 11/12 节已更新。
- 打包：PyInstaller 6.22.2 按 work_dashboard.spec 产出 `dist/WorkDashboard.exe`（18MB），冒烟测试通过（health/export/前端按钮均正常，用户 dist/data/works.db 数据保留）。

## 环境坑（重要）

- 本机 Windows 上部分端口被系统保留（8765 绑定报 winerror 10013），测试后端换 8899 成功。
- **PyInstaller 重复构建会因 sitecustomize 安全删除 shim 失败**：os.remove 旧 build/dist 文件时走回收站失败即中断。解决：构建前先把 `build/work_dashboard` 移走（mv 改名，不走删除）并确保 `dist/WorkDashboard.exe` 已不存在，一次性干净构建即可成功。bash rm 有时也被 shim 拦（trash 失败时 fail-closed），用 mv 绕开。
- venv 路径：`C:/Users/john/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（已装 fastapi/uvicorn/pyinstaller）。

## 工作台四项 UX 优化（第二次迭代）

1. **按类型独立的录入记忆**：`modal.js` 记忆键改为 `wd_last_regular` / `wd_last_other` 两个，互不影响；bindTypeSeg 增加 onChange 回调，新建弹窗切换类型时先把当前输入存回原类型再载入新类型记忆；「清空重填」只清当前类型；保存时按 workType 归档记忆。
2. **科技感背景**：`index.html` 新增 `.bg-fx` 装饰层（bg-stars 星点闪烁 / bg-beam 光束扫过 / bg-grid-floor 底部透视网格），CSS 变量 `--star`、`--grid-line` 深浅主题各配色，浅色主题降低透明度，prefers-reduced-motion 降级。
3. **统计报表重构**：`stats.js` renderHero → renderReport，三张 report-card（常规工作/其他工作/总统计），各含总工时/总收入/小时工资三个指标；CSS 新增 .report-card/.rc-*/.rm-* 样式（类型色顶条 + HUD 角标），.panel 也加了角标。
4. **细节**：修复 .restore-hint 未定义变量 --hint-bg（改用 rgba）；移动端报表卡字号收紧。
- README 功能表/标语已同步；服务验证（curl 资源 + JS 语法检查）通过后重新打包 exe。

## 第三次迭代：全量逻辑审查 + 最终打包

- 排查「修改完成日期后报表收入未变」：确认为**周报表口径**（收入按完成日期归入所在自然周，同周内改日期数字不变），非 bug；已实测跨周修改正确迁移。
- 全量审查后修复 3 处真实隐患：
  1. `works.py complete`：回退优先级改为「本次提交 → 已有实际值（done 状态）→ 计划值/今天」，重复调用不再把已填实际值覆盖回计划值（实测仅传日期重调接口，实际时长/收入保留）；
  2. `data_io.py _normalize_row`：导入的 done 工作缺完成日期回退计划日期、缺实际值回退计划值，避免从统计/看板消失；
  3. `run_exe.py` HOST 默认 `0.0.0.0` → `127.0.0.1`（exe 仅本机访问，防数据暴露局域网）；`main.py` 增加 HOST 环境变量支持（Docker 默认 0.0.0.0 不变）。
- 回归测试全过：增删改查/完成/部分重调/重开、周/日统计数学核对、导入归一化、导出往返、前端资源与 JS 语法。
- 文档同步：API 文档第 5/9/12 节（回退规则表、周口径、导入归一化）、README（HOST 变量、目录结构补 data_io.py）、架构设计（routers 职责）、部署指南（exe 仅本机访问说明）。
- 又发现一个被保留端口：8895（连接拒绝但进程存活），可用端口 8893/8896/8897/8898/8899。
